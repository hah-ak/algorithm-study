#첫시작
함수
언팩연산자 (*)
: 파라미터의 갯수를 마음대로 할 수있다.
: 튜플형식으로 나온다
: 반환값이 여러개일 수있다.
키워드 언팩 연산자 (**)
:파라미터 갯수를 가변적으로 사용할 수 있도록하며
:매개변수를 딕셔너리 형식으로 처리함
함수에 기본 매개변수를 쓸 수있다 (def func(a,b,c="+"):)
: 파라미터를 쓰지 않은 경우 실행된다.
: 즉 파라미터 갯수가 맞지 않아도 실행됨.
: 파라미터를 받지 않고 함수에 지정된것.
scope
: 변수의 유효범위
global
: 전역 변수가 지역변수에 의해 지워질 때, global사용해
nonlocal
: outer 영역에서 해당 변수를 찾음

전역변수 호출
클로저
:정보은닉을 위해 함수에 함수를 쓰는 방식.(중첩함수)
instance
:
map
filter
eval
enumerate
pow
all
any
zip
globals
locals

#제발 되자